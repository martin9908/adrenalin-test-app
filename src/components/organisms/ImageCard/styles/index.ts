/**
 *
 * @format
 * @flow
 *
 */

import {ScaledSheet} from 'react-native-size-matters';

const ImageCardStyles = ScaledSheet.create({
  iconContainer: {
    marginTop: '41@s',
    paddingLeft: '40@s',
    alignItems: 'flex-start',
    flexDirection: 'row',
  },
});

export default ImageCardStyles;

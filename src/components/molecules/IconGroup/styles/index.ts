/**
 *
 * @format
 * @flow
 *
 */

import {ScaledSheet} from 'react-native-size-matters';

const IconGroupStyles = ScaledSheet.create({
  icon: {
    marginRight: '8@s',
  },
});

export default IconGroupStyles;

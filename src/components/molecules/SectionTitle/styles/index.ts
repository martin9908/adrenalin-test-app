/**
 *
 * @format
 * @flow
 *
 */

import {ScaledSheet} from 'react-native-size-matters';

const SectionTitleStyles = ScaledSheet.create({
  container: {
    marginTop: '20@s',
    marginBottom: '10@s',
    justifyContent: 'space-between',
    flexDirection: 'row',
  },
});

export default SectionTitleStyles;
